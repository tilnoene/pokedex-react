import React from 'react';
import Button from '../Button';

import './styles.css';

const Header = () => (
    <header id="main-header">
        <h1>Focadex</h1>
        <Button title={"Perfil"}/>
    </header>
);

export default Header;