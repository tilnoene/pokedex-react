import React from 'react';
import Button from '../Button';

import './styles.css';

const MainMenu = () => (
    <header id="main-footer">
        <Button title={"<"}/>
        <Button title={">"}/>
    </header>
);

export default MainMenu;